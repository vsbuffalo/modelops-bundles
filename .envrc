# direnv configuration for modelops-bundles

# Set Docker host for OrbStack on macOS
# This allows testcontainers and other Docker tools to work properly
export DOCKER_HOST="unix://$HOME/.orbstack/run/docker.sock"

# Optional: Add any other project-specific environment variables here
# export MODEL_OPS_CACHE_DIR="$PWD/.cache"
# export PYTHONPATH="$PWD/src:$PYTHONPATH"

echo "✅ Docker host set to OrbStack socket"